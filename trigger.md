Current directory is the root of a Linux Kernel git repository.

Read the prompt review/agent/orc.md

Analyze commit HEAD (${SHA}) using prompts from review/

This commit is part of a series with git range ${BASE_SHA}..${HEAD_SHA}
